<h1 class="mt-5">{{category.name}}:</h1>

{{#each posts}}
    <div class="card mt-4">
        <div class="card-body">
            <small>Publication date: {{date}} </small>
            <h3 class="mt-3">{{title}} </h3>
            <p class="mt-3">{{description}} </p>
            <small>Category: {{name}} </small><br>
            <a href="/post/{{slug}}"><button class="btn btn-success mt-3">Ler Mais</button> </a>
        </div>
    </div>
{{else}}
{{/each}}