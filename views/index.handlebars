<div class="jumbotron">
  <h1 class="display-4">Welcome to my Blog!</h1>
  <p class="lead">Here you have current information about the programming market and technologies!</p>
  <hr class="my-4">
  <p>Create your account and receive current news.</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/users/register" role="button">Create account</a>
  </p>
</div>

<hr>

<h1>Recents Posts</h1>
<div class="mt-5">
    {{#each posts}}
            <div class="card mt-4">
                <div class="card-body mt-2">
                    <small>Publication date: {{date}} </small>
                    <h3 class="mt-3">{{title}} </h3>
                    <p class="mt-3">{{description}} </p>
                    <small>Category: {{category.name}} </small><br>
                    <a href="/post/{{slug}}"><button class="btn btn-success mt-3">Ler Mais</button> </a>
                </div>
            </div>
    {{else}}
        <h4>None post registered.</h4>
    {{/each}}
</div>