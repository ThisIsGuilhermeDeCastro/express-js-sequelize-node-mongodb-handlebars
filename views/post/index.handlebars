
<div class="card">
    <div class="card-body">
        <h1 class="mt-5">{{post.title}}</h1>
        <hr>
        <small>Publication date: {{post.date}} </small><br>
        <div class="mt-5">
            {{post.content}}
        </div>

    </div>
</div>